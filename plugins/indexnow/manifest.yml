name: netlify-plugin-indexnow

inputs:
  - name: key
    description: IndexNow ownership key published at the site root.
    required: true
  - name: host
    description: Canonical hostname whose changed URLs should be submitted.
    required: true
